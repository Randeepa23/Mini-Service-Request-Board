import type { Metadata } from "next";
import { Inter } from "next/font/google";
import "./globals.css";

const inter = Inter({
  variable: "--font-inter",
  subsets: ["latin"],
  display: "swap",
});

export const metadata: Metadata = {
  title: "ServiceBoard — Mini Service Request Board",
  description:
    "Post and manage home service requests. Connect homeowners with tradespeople for plumbing, electrical, painting, and joinery jobs.",
  keywords: ["service requests", "tradespeople", "plumbing", "electrical", "home services"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${inter.variable} h-full`}>
      <body className="min-h-full flex flex-col antialiased">{children}</body>
    </html>
  );
}
